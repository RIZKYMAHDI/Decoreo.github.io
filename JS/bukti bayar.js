const backButton = document.querySelector('.button-container button[type="submit"]');

backButton.addEventListener('click', () => {
    backButton.style.background = '#4e5669';
    backButton.style.color = '#fff';
});